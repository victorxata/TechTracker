'use strict';
angular.module('txAdmin.fakeBackend', ['ngMockE2E'])
    .run(function(/*$httpBackend*/){

        /*$httpBackend.when('POST', '/stuff').respond(function(method, url, data){
            console.log('GET /stuff', data);
            return [200, stuff, {}];
        });*/

    });